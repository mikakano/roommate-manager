# 合租生活管家 PRD

版本：v1.0 · 首版演示雏形

已确认：首版只需完成雏形，不需要真实多人使用。

## 1. 概述

合租生活管家帮助室友看清共同费用、安排公共区域清洁、记录公共物品，并维护共同生活规则。首版以一个预置合租家庭展示完整的基础操作，供面试官通过公开网页体验。优先在约15分钟内完成可部署版本，在题目允许的48小时内持续完善。

## 2. 参与角色

| 角色 | 职责 | 备注 |
| --- | --- | --- |
| 考生／产品负责人 | 确认范围、检查体验、提交链接 | 姓名不预设 |
| AI Coding 工具 | 根据本文实现页面、交互及部署配置 | 不添加超出首版范围的服务 |
| 面试官 | 访问网页，评估产品设计和操作体验 | 无需账号即可体验 |

## 3. 背景

合租生活中，费用记录分散会导致“谁垫付、谁该付、付了没有”难以核对；清洁责任不明确容易遗漏；公共物品缺少消耗记录会出现断货；生活规则仅靠口头沟通容易产生不同理解。

本项目来自限时笔试附加题。题目要求覆盖费用AA、清洁值日、公共物品登记与提醒、室友公约管理，且仅提交一个公开部署链接。首版用于展示产品结构与基础功能，不用于管理真实账务。

硬性约束：

- 必须部署上线，不能仅本地运行或提交代码仓库链接。
- 提交的网站地址保持不变，后续在同一地址更新。
- 面试官无需登录、申请访问权限即可打开。
- 首版不需要真实多人使用，数据仅保存在当前浏览器。

## 4. 目标与成功标准

### 产品目标

让用户打开首页就知道当前合租生活中有哪些费用、值日和补货事项，并能完成四项核心功能的基本操作。

### 首版验收目标

| 目标 | 可验证结果 | 时间范围 |
| --- | --- | --- |
| 交付可访问作品 | 公开网站在无痕窗口中可打开，无需登录 | 首版提交前 |
| 覆盖题目四项需求 | 四个模块都有示例内容和至少一个有效操作 | 首版提交前 |
| 展示准确分摊 | 新增账单后立即显示各参与者金额，分摊总和等于账单金额 | 首版提交前 |
| 提供持续演示体验 | 修改后刷新仍保留；恢复演示数据后回到初始状态 | 首版提交前 |
| 方便评审浏览 | 首页能看到费用、今日值日和低库存提示；手机宽度无明显横向溢出 | 首版提交前 |

这些是作品验收指标，不作为真实用户增长或矛盾减少的已验证结论。

## 5. 用户与使用场景

| 用户场景 | 希望完成的事 | 当前困扰 |
| --- | --- | --- |
| 垫付公共费用的室友 | 登记费用，明确谁应偿还多少钱 | 手算、聊天记录难核对 |
| 参与公共区域维护的室友 | 看清自己的任务，记录完成 | 排班不清、责任不明 |
| 购买或使用公共物品的室友 | 记录库存，发现补货需求 | 消耗无记录、临时断货 |
| 共同制定规则的室友 | 查看并修改共同生活规则 | 约定散落在聊天中 |
| 体验作品的面试官 | 快速理解并试用四项功能 | 空页面、登录门槛会阻碍评审 |

## 6. 用户价值

- 费用：将账单、付款人、参与者和偿还状态集中展示，减少核对成本。
- 清洁：将公共区域任务落实到具体室友和日期，避免责任悬空。
- 物品：用库存及低库存提示说明补货需求，减少断货。
- 公约：让共同规则有统一入口，便于查看和维护。

以上价值来自题目痛点与产品假设，尚未通过用户访谈或对比研究验证。首版不宣称已经减少真实室友矛盾。

## 7. 方案与功能需求

### 7.1 页面结构和基础体验

应用名称：合租生活管家。预置家庭：阳光合租屋。预置室友：小林、小陈、小周、小许，默认当前室友为小林。

导航包含：首页、费用AA、清洁值日、公共物品、室友公约。使用同一页面内的模块切换，避免静态部署下直接访问子路径导致页面无法打开。

电脑端使用清晰的顶部导航；手机端使用可点击且不拥挤的导航。整体采用温暖清爽的配色、卡片和明确的状态文字，颜色不能是区分状态的唯一方式。

通用规则：

- 顶部允许切换当前室友；切换仅改变查看视角，不是账号登录，也不代表权限隔离。
- 预置数据保证首次打开即可体验全部模块，值日日期围绕首次初始化时的当前周生成。
- 页脚注明“演示版：数据仅保存在当前浏览器，不会同步给其他室友”。
- 提供“恢复演示数据”按钮，确认后覆盖本地修改，重新生成示例数据。
- 操作成功提供简短反馈；无数据时展示说明和操作入口。
- 输入框提供明确字段名，按钮可用键盘操作，正文及状态文字保持可读对比度。

核心流程：

1. 登记费用 → 输入金额和付款人 → 选择参与室友 → 查看分摊预览 → 保存 → 更新账单和首页汇总。
2. 查看值日 → 找到当天任务 → 标记完成 → 首页待办减少；可撤销完成。
3. 登记物品／记录消耗 → 更新库存 → 达到低库存条件 → 显示补货提示 → 补货后取消提示。
4. 查看公约 → 新增或编辑内容 → 保存 → 列表显示最新内容。

### 7.2 功能明细

以下均为首版必需功能。除特别说明外，当前浏览器中的体验者均可操作。

#### A. 首页

- 展示合租屋名称、当前室友及当月公共费用总额。
- “我待结清”：汇总当前室友应偿还给其他付款人的未结清金额，不包含当前室友自己的垫付或本人份额。
- “今日值日”：展示当前室友今天未完成的任务，可进入清洁模块。
- “待补货”：展示低库存物品数量及名称，可进入物品模块。
- 展示最近账单和快捷新增费用入口。
- 未偿还费用跨月继续计入“我待结清”，避免被当月筛选隐藏。

#### B. 费用AA

字段：账单名称、分类（房租／水电／公共采购／其他）、金额、费用日期、付款人、参与室友。名称、金额、日期、付款人和至少一名参与者必填。

- 支持新增账单；首版仅支持所选参与者等额分摊。
- 金额必须大于0，最多两位小数；允许付款人不参与分摊。
- 将金额转为整数“分”计算：每人先分配整除后的金额，剩余的分按预置室友顺序依次分配给参与者，确保总和准确。
- 保存前展示每人分摊预览，例如100元由3人分摊为33.34元、33.33元、33.33元。
- 账单展示付款人、参与者、各自份额及偿还状态。
- 付款人的本人份额标为“本人承担”，无需偿还；其他参与者默认“待结清”。
- 可将某个参与者的份额标为“已结清”，也可撤销；这仅是记录状态，不触发真实支付。
- 全部需要偿还的份额已结清时，账单为“已结清”；没有需要偿还的份额时，显示“无需偿还”。
- 提供当月／全部账单查看；日期以账单费用日期为准。
- 不做不同账单之间的债务抵扣。错误账单可确认删除后重新新增；删除同步移除其分摊记录。

验收例：小陈付款100元，参与者为小林、小陈、小周。小林应偿还小陈33.34元，小陈承担33.33元，小周应偿还33.33元；小林标记结清后，其首页待结清金额减少33.34元。

#### C. 清洁值日

字段：任务名称、公共区域、值日日期、负责人、完成状态。公共区域预置厨房、客厅、卫生间。

- 展示本周7天的示例排班，按室友顺序轮换负责人；未安排任务的日期显示“无任务”。
- 支持新增任务，填写任务名称、区域、日期并选择一名负责人。
- 支持修改任务的负责人，便于演示换班。
- 支持标记完成和撤销完成。
- 当天未完成显示“待完成”；日期早于今天且未完成显示“已逾期”；完成后显示“已完成”。
- 展示下一项已安排任务的日期及负责人；无后续任务时显示“暂无后续安排”。
- 本周列表与首页使用同一份任务数据，状态保持一致。
- 首版不做无限期自动排班、换班审批、处罚或排行榜。

验收例：当前室友将今天的厨房清洁标记完成，任务立即显示已完成，首页今日待办同步减少；撤销后恢复待完成。

#### D. 公共物品

字段：物品名称、剩余数量、单位、低库存阈值。示例包含纸巾、垃圾袋、洗洁精，至少一项初始为低库存。

- 支持新增物品；名称和单位必填，库存和阈值为非负整数。
- 以包、卷、瓶等整件单位登记，不估算半瓶或百分比余量。
- 支持输入消耗数量，必须为正整数且不超过当前库存；提交后扣减库存。
- 支持输入补货数量，必须为正整数；提交后增加库存。
- 库存小于或等于阈值时显示“需要补货”；库存为0时显示“已用完，需要补货”。
- 补货使库存高于阈值后，低库存提示和首页计数同步更新。
- 提醒仅在网页内显示，不发送短信、邮件或推送。
- 首版不做采购订单、消耗历史或自动关联费用账单。

验收例：垃圾袋库存2卷、阈值1卷，消耗1卷后进入待补货；补货3卷后库存为4卷，待补货提示消失。

#### E. 室友公约

字段：标题、正文、更新时间。预置安静时间、访客规则、公共区域使用规则。

- 支持新增和编辑，标题及正文不能为空或仅包含空白。
- 保存后更新列表内容和更新时间。
- 支持确认删除公约；取消时保留原内容。
- 默认公约示例：23:00后减少噪音；访客留宿提前沟通；使用厨房后及时清理。
- 公约只表示共同生活约定，不表示法律效力或真实室友已同意。
- 首版不做投票、签署或版本历史。

### 7.3 数据及部署

- 首版为静态前端网页，部署目标为 GitHub Pages，不需要服务器和数据库。
- 数据存入浏览器本地存储，包含家庭信息、室友、账单及份额、清洁任务、物品、公约。
- 保存数据版本号，后续更新时优先兼容已有数据，避免每次打开覆盖用户操作。
- 首次无数据时载入示例；再次访问时读取本地数据。
- 读取失败时提示恢复演示数据；存储不可用时允许在当前页面继续操作，并提示刷新后可能丢失。
- 使用本地日期计算今日、本周和当月，避免时区转换造成日期偏移。
- 不上传真实个人信息，不在前端文件放入密码或密钥。
- 使用自包含的样式与图标，减少对外部资源的依赖。
- 静态资源路径适配项目网址中的仓库目录；如使用构建工具，配置正确的发布目录和资源基础路径。
- 提交正式公开网站地址；保留同一仓库、发布设置、用户名和项目名，持续更新同一网址。

### 7.4 假设与边界

已确认：首版是雏形，不需要真实多人使用。

默认设计假设：

- 一个合租屋、四名室友足以展示主要流程，首版不做成员增删和多个家庭。
- 等额分摊能覆盖核心演示场景，自定义比例留到后续。
- 首版由体验者手动维护状态，无需校验其他室友是否真的付款或完成任务。
- 页面内补货提示满足首版提醒需求，真实通知留到后续。

首版不包含：注册登录、跨设备同步、真实支付、真实消息通知、权限控制、附件上传或真实财务报表。

## 8. 发布计划与验收清单

### 首版：约15分钟实施目标

| 相对时间 | 交付内容 |
| --- | --- |
| 0—2分钟 | 确认发布入口、搭建页面结构和示例数据 |
| 2—7分钟 | 完成四个模块和关键操作，优先验证费用计算 |
| 7—10分钟 | 首次部署，取得固定公开网址 |
| 10—13分钟 | 在线检查核心流程，修复明显错误 |
| 13—15分钟 | 无痕访问、手机宽度检查、确认提交地址 |

这是实施目标，不保证部署等待或账号准备耗时。若时间紧张，先简化动画和装饰，保留四项功能、正确计算和公开部署。

### 提交后至题目允许的48小时内

- 优先修复线上错误、完善手机体验、补齐空状态和输入提示。
- 其次增强费用编辑、任务编辑与删除、物品编辑及操作记录。
- 有余力再增加自定义分摊和可配置排班规则。
- 保持原提交网址不变；任何新增功能不得破坏现有演示流程。
- 真实多人协作属于后续产品方向，不是本次首版交付承诺。

### 提交前检查

- [ ] 正式网站在无痕窗口中打开，无需登录。
- [ ] 提交网址指向作品页面，而非仓库或本地服务。
- [ ] 首页及四个模块均有合理示例数据。
- [ ] 100元由3人分摊，总额仍为100元。
- [ ] 结清和撤销操作同步更新首页金额。
- [ ] 值日完成和撤销操作同步更新首页待办。
- [ ] 库存不会变成负数，补货提示随库存变化。
- [ ] 公约可新增、编辑和删除，空内容不能保存。
- [ ] 刷新后修改仍在；恢复演示数据需确认并可生效。
- [ ] 手机宽度下主要信息和按钮可正常查看与操作。
- [ ] 已记录固定发布网址，后续保持网址及发布入口不变。
